#include <stdio.h>

int checksum(int) {
    // every other digit by 2, starting with second-to-last digit
    // Add sum to the sum of digits that weren't multiplied together
    // if total's last digit is 0, the number is valid
}

int main(void) {
    print("Enter Credit Card #: ");
    int ccNum
    // prompt for input

    // calculate getsum

    // check card length an starting digits

    // print AMEX, MASTERCARD, VISA, or INVALID
    // American Express: 15-digits, start with 34 or 37
    // MasterCard: 16-digits, start with 51, 52, 53, 54, or 55
    // Visa: 13- and 16-digits, start with 4

}
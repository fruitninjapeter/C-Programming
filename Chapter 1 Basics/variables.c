#include <stdio.h>
#include <stdlib.h>

int main() {
    char name[] = "Peter";
    int age = 24;
    printf("His name is %s and he is %d years old.\n", name, age);
    printf("Sometimes %s feels like he doesn't know much.\n\n", name);
    printf("But at %d he's still very capable :)\n", age);
    printf("Gradually %s will create the conditions\nto move on to the next stage with the one he cherishes.\n", name);
    printf("He'll take one step at a time in the journey ahead.\n");
}